<UserControl
    x:Class="RedgifsDownloader.View.DupePicsCleanerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:RedgifsDownloader.Converter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:RedgifsDownloader.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <UserControl.Resources>
        <conv:InverseBoolConverter x:Key="InverseBoolConverter" />
    </UserControl.Resources>
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            Text="路径:" />
        <TextBox
            Grid.Row="0"
            Grid.Column="1"
            Margin="5,0,0,0"
            Text="{Binding TargetFolder, UpdateSourceTrigger=PropertyChanged}" />
        <Button
            Grid.Row="0"
            Grid.Column="2"
            MinWidth="50"
            Margin="5,0,0,0"
            Command="{Binding BrowseCommand}"
            Content="选择" />
        <Button
            Grid.Row="1"
            Grid.Column="1"
            MinWidth="90"
            Margin="5,10,0,0"
            HorizontalAlignment="Left"
            Command="{Binding DeleteCommand}"
            Content="删除所有重复"
            IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}" />
        <Button
            Grid.Row="1"
            Grid.Column="1"
            MinWidth="90"
            Margin="0,10,0,0"
            HorizontalAlignment="Right"
            Command="{Binding RenameCommand}"
            Content="重命名" />
        <GroupBox
            Grid.Row="2"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            Margin="0,10,0,0"
            Header="日志">
            <TextBox
                Margin="0,5,0,0"
                Foreground="Gray"
                IsReadOnly="True"
                Text="{Binding ResultMessage, UpdateSourceTrigger=PropertyChanged}"
                TextWrapping="Wrap"
                VerticalScrollBarVisibility="Auto" />
        </GroupBox>
    </Grid>
</UserControl>
