<UserControl x:Class="RedgifsDownloader.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RedgifsDownloader.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <GroupBox Grid.Row="0"
                  Header="下载"
                  Margin="5"
                  VerticalAlignment="Top">
            <Grid Margin="0,15,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition MaxWidth="70"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" 
                           Text="下载路径:"
                           Margin="5,0,0,0"/>
                <TextBox Grid.Column="1" x:Name="DirectoryBox"
                         MaxHeight="30"
                         Margin="5,0,0,0"
                         Text="{Binding DownloadDirectory}"/>
                <Button Grid.Column="2" x:Name="ChooseBtn"
                        Content="选择"
                        MaxHeight="30"
                        Margin="5,0,0,0"
                        Command="{Binding ChooseDownloadFolderCommand}"/>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
