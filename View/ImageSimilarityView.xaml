<UserControl
    x:Class="RedgifsDownloader.View.ImageSimilarityView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:RedgifsDownloader.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
    d:DesignHeight="450"
    d:DesignWidth="330"
    mc:Ignorable="d">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            VerticalAlignment="Center"
            Text="路径" />
        <TextBox
            Grid.Row="0"
            Grid.Column="1"
            Margin="5,0,0,0"
            VerticalContentAlignment="Center"
            Text="{Binding FolderPath, UpdateSourceTrigger=PropertyChanged}" />
        <Button
            Grid.Row="0"
            Grid.Column="2"
            MinWidth="70"
            Margin="5,0,0,0"
            Command="{Binding GetDirectoryCommand}"
            Content="选择" />
        <TextBlock
            Grid.Row="1"
            Grid.Column="0"
            Margin="0,10,0,0"
            VerticalAlignment="Center"
            Text="阈值" />
        <xctk:DoubleUpDown
            Grid.Row="1"
            Grid.Column="1"
            Width="70"
            Height="20"
            Margin="5,10,0,0"
            HorizontalAlignment="Left"
            HorizontalContentAlignment="Left"
            VerticalContentAlignment="Center"
            FormatString="F2"
            Increment="0.01"
            Maximum="1.00"
            Minimum="0.01"
            Value="{Binding Threshold, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <Button
            Grid.Row="2"
            Grid.Column="1"
            MinWidth="80"
            Margin="5,10,0,0"
            HorizontalAlignment="Right"
            Command="{Binding ExecuteCommand}"
            Content="开始筛选" />
        <Button
            Grid.Row="2"
            Grid.Column="2"
            MinWidth="70"
            Margin="5,10,0,0"
            HorizontalAlignment="Right"
            Command="{Binding GoToFolderCommand}"
            Content="前往文件夹" />
        <GroupBox
            Grid.Row="3"
            Grid.ColumnSpan="3"
            Margin="0,10,0,0"
            Header="日志">
            <TextBox Text="{Binding Logger, UpdateSourceTrigger=PropertyChanged}" />
        </GroupBox>
    </Grid>
</UserControl>
